postgres:
  image: postgres:latest
  expose:
    - 5432
  restart: always
  volumes:
    - /mnt/storage/HOME/data/postgres:/var/lib/postgresql/data
    - /data:/data:ro
  environment:
    POSTGRES_PASSWORD: root
    POSTGRES_USER: root
